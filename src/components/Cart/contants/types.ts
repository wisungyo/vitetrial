export type TypeCart = {
  id: string;
  title: string;
  description: string;
  price: number;
  currency: string;
  quantity: number;
  image: string;
  label: string;
  status: string;
  biddingRound: number;
};
